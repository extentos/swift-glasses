// SherpaOnnxC — SPM C target exposing sherpa-onnx's C API header (pinned
// v1.13.4; include/sherpa-onnx-c-api.h is committed, the binaries are the
// gitignored Artifacts/ xcframeworks installed by
// scripts/install-sherpa-ios.sh). This file exists because SPM requires a
// compilable source in a C target; the symbols come from the xcframeworks.
